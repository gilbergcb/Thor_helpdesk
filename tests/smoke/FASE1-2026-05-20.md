# Fase 1 — Deploy Smoke — 2026-05-20 23:25 GMT-3

**Branch:** `security/convergence-batch-1` (commit `356c988`)
**VPS:** `finanpersona-vps:/home/finadmin/winthor-helpdesk`
**Modo de todas as flags F-01/F-02/F-03:** `audit` (default seguro)

## Flags ativas

| Env | Valor | Comportamento |
|---|---|---|
| `SECURITY_MEDIA_AUTH` | `audit` (default) | loga acesso sem token; não bloqueia |
| `SECURITY_WEBHOOK_HMAC` | `audit` (default) | loga Client-Token ausente/divergente; não bloqueia |
| `SECURITY_SSRF_ALLOWLIST` | `audit` (default) | loga download de host fora da allowlist; não bloqueia |

## Validação pós-deploy

| Teste | Esperado | Obtido | Status |
|---|---|---|---|
| `/health` | `200 {"status":"ok"}` | idem | ✅ |
| `docker compose ps` | 3 serviços Up | backend Up (recreated), db healthy, frontend Up | ✅ |
| Login senha errada | `401` | `401` | ✅ |
| F-01 audit: `GET /media/999999` sem token | `404` + log `security.audit check=media.auth reason=missing_token` | idem | ✅ |
| F-02 audit: `POST /webhooks/zapi` sem Client-Token | `200` + log `security.audit check=webhook.zapi reason=missing_client_token` | idem | ✅ |
| F-02 audit: `POST /webhooks/zapi` com Client-Token correto | `200` SEM log de audit | idem (nenhum audit log emitido) | ✅ |

## Próximos passos

1. **Janela de 72h em audit**: monitorar logs do backend buscando `security.audit`. Se aparecer:
   - `media.auth missing_token` de IPs legítimos → frontend precisa enviar Bearer antes de flipar `enforce`.
   - `webhook.zapi client_token_mismatch` → revisar config do Z-API antes de flipar `enforce`.
   - `ssrf.download host_not_allowlisted` → expandir `SSRF_ALLOWED_HOSTS` antes de `enforce`.
2. Smoke manual do Gilberg:
   - `#chamado teste fase 1` em grupo Z-API real → confirmar ticket cria.
   - Enviar imagem → confirmar render no TicketDrawer.
3. Após 72h sem audit problemático: criar Fase 1b (flip dos defaults para `enforce` no `.env` do VPS).

## Rollback

Se algo regredir, em ~10s:
```bash
ssh finanpersona-vps
cd /home/finadmin/winthor-helpdesk
# adicionar ao .env:
echo 'SECURITY_MEDIA_AUTH=off'      >> .env
echo 'SECURITY_WEBHOOK_HMAC=off'    >> .env
echo 'SECURITY_SSRF_ALLOWLIST=off'  >> .env
docker compose restart backend
```
Rollback definitivo: rsync do commit anterior (`050d693`) + rebuild backend (ver `RUNBOOK.md`).
