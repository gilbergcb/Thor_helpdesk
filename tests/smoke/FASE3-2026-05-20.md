# Fase 3.2 + 3.3 — Deploy Smoke — 2026-05-20 23:34 GMT-3

**Branch:** `security/convergence-batch-1` (commit `1699e9f` + fix `cryptography` dep)
**Escopo:** F-07 (python-jose → PyJWT) e F-13 (startup assert JWT secret).
**Adiado:** F-08 (rotação de segredos) — depende de Phase 2.2 (dual-key vault).

## Mudanças

| Item | Arquivo | Comportamento |
|---|---|---|
| F-07 | `app/core/security.py`, `pyproject.toml` | Import `jwt` (PyJWT 2.12.1) substitui `python-jose`. Mesma assinatura HS256, mesmo secret → tokens emitidos pela versão anterior continuam válidos. **Nenhum logout forçado.** |
| F-13 | `app/core/config.py` | `model_validator` em `Settings` rejeita boot em `environment=production` se `JWT_SECRET_KEY` for default (`change-me-in-production`, `secret`, vazio) ou < 32 chars. Em dev/staging: só WARNING. |
| Fix | `pyproject.toml` | Adicionado `cryptography>=43.0.0` explícito (era transitive de `python-jose[cryptography]`, ainda necessário para Fernet do vault). |

## Incidente

Primeiro deploy quebrou o backend com `ModuleNotFoundError: No module named 'cryptography'` porque `python-jose[cryptography]` puxava `cryptography` transitivamente. Corrigido em ~2 min adicionando dep explícita e rebuildando. Tempo de downtime: ~90s (backend só, db e frontend OK).

## Validação pós-deploy

| Teste | Esperado | Obtido | Status |
|---|---|---|---|
| `/health` | `200 {"status":"ok"}` | idem | ✅ |
| Container `backend` | Up healthy | Up 5s after rebuild | ✅ |
| Login senha errada | `401` sem stack | `401 {"detail":"Invalid email or password"}` | ✅ |
| Round-trip JWT em container | `create_access_token` → `decode_access_token` retorna mesmo sub | sub=42, lib=jwt 2.12.1 | ✅ |
| Token inválido | retorna `None` (não raise) | `None` | ✅ |
| Startup logs | Sem ERROR/Traceback, sem F-13 WARNING (JWT secret forte no .env) | limpo | ✅ |

## Próximos passos

1. **Phase 2.2 (dual-key vault) antes de F-08**: implementar suporte a `VAULT_SECRET_KEY` + `VAULT_SECRET_KEY_OLD` em `_vault()`; criar comando `re_encrypt_all_credentials()`.
2. Só depois: janela de rotação de segredos (Phase 3.1 / F-08).
3. Validação F-13 negativa (opcional): rodar localmente com `JWT_SECRET_KEY=short` e `ENVIRONMENT=production` → deve falhar boot.

## Rollback

`docker compose` no VPS pode usar a imagem anterior se rotulada, mas como build é in-place, o caminho é:
```bash
ssh finanpersona-vps
cd /home/finadmin/winthor-helpdesk
git -C /tmp/helpdesk-backup checkout 050d693 -- app/core/security.py app/core/config.py pyproject.toml || \
  (rsync do dev a partir do commit 050d693)
docker compose build backend && docker compose up -d backend
```
